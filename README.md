# SpendFox Android

Native Android prototype for SpendFox.

The project uses Kotlin, Jetpack Compose and local persistence. Cloud synchronization is intentionally deferred until the local app model is stable.
